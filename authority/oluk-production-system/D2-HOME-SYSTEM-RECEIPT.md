# D2 homepage/system extraction receipt

Status: `SOURCE_VALIDATED_UNINTEGRATED`

- Design base: `bbef23d2cc673965f21f8e60b6456ce5dd02f588`
- Structure source: `7fc8bcd46e5d9ffa3489455ecd7b04f14d6ba9a0` as `ADAPT_STRUCTURE_ONLY`
- System index: `9867e292f12b9805df9d085dc0a5dbbc718ce45b` as `EVIDENCE_INDEX_ONLY`
- Runtime registry: `OLUK_RUNTIME_CONSUMPTION_REGISTRY_V1@1.3.0`

The five-position stage keeps its accepted layout, URL selection/restoration,
keyboard, loading, reduced-motion, and mobile recomposition behavior. Product,
metric, price, link, media, and customer-copy inputs are now consumer supplied.
The component suppresses itself unless it receives five complete canonical
product inputs plus owner-approved content; it does not restore fixture values.

The old disabled fixture purchase action, literal prices and product values,
verification/testing claims, record-availability copy, and Figma node markers
were removed. Status presentation now defaults fail closed and renders
`OPENLAB REPORTED` only for the legacy evidence state that represents a bound
reported result. `OPENLAB VERIFIED` and `RECORD AVAILABLE` are forbidden.

The system-page registry remains an evidence index. It does not add customer
routes, a Sites runtime, Figma work, dark-mode work, or commerce/OPENLAB wire
schemas. Production Next binding and four-width human visual acceptance remain
downstream gates.

The tracked grammar generator was run after a clean build. It correctly made
no write because the merged D1 base already reports 489 untouched-route copy
violations against the committed 487 baseline across four collection routes.
The unmodified D1 worktree reproduces the same 489 count, so this is classified
as pre-existing generated-audit debt rather than a D2 source regression.
