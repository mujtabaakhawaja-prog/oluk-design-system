# Sites System Atlas topology

**Status:** owner-only Codex Sites staging. Native Next remains the sole customer runtime.

The Atlas consumes the generated OLUK_SURFACE_INVENTORY_V1, OLUK_PRESENTATION_SYSTEM_V2, and OLUK_ROUTE_PRESENTATION_AUTHORITY_V1 artifacts. Its route list is imported from the pinned Native Next 74-route registry. It does not maintain an independent route ledger.

The bounded shell structure was compared with Design commit 702e17d1b796726299b9dceeca39a8bcd0cbeaa2. That commit is not cherry-picked because its parent and component graph are stale. The active Atlas reuses only the owner-tooling route, contained section grammar, responsive inventory grid, and summary treatment.

Figma links may appear as optional provenance in later evidence views. They are not construction dependencies, generated-contract inputs, or approval gates.
