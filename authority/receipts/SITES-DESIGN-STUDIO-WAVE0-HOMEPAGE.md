# SITES DESIGN STUDIO WAVE 0 — HOMEPAGE

Status: `CANDIDATE · HUMAN_HOMEPAGE_DESIGN_APPROVAL_PENDING`

## Source

- Repository: `oluk-design-system`
- Branch: `codex/oluk-sites-design-studio-v1`
- Base: `6044eaec79903909daf36de487c07b555864bb1c`
- Local review: `http://127.0.0.1:4192/`
- Customer/runtime authority: none

## Locked operating model

```text
Sites System Atlas
  identity-system input, census, grammar, states and route makeup

Sites Review Studio
  creative output frontier, comparison, annotation and approval

Native Next Runtime Studio
  selected-candidate parity proof in fixture and real-adapter modes

Native Next customer routes
  same approved grammar with server-owned adapters and no review chrome
```

Figma remains provenance through MF/WS inputs. Repository-owned bridge metadata is evidence, not native Code Connect enforcement and not customer runtime authority.

## Current explicit corrections

- `OPENLAB VERIFIED` plus its icon is correct for product evidence status surfaces.
- MF-02B remains the preferred adaptive/card-family input; MF-03 is a separate OpenLab Portal mockup.
- The OpenLab Portal defect is inconsistent shared card/elevation grammar, not the word `VERIFIED`.
- The current 380-entity surface inventory is infrastructure and is not an exhaustive component census.
- `HeroDecisionSurface`, `PdpFirstFold` / `PdpDecisionHero`, `PdpMediaChamber`, `ProductMediaChamber`, and `PurchasePanel` remain separate named levels.

## Wave 0 Homepage slice

This candidate stages:

- a raised white `HeroCopyCard`;
- a distinct raised white `HeroDecisionSurface`;
- five-SKU selection for MK-2866, MENT, ENDURASHRED, RAD-140 and MK-677;
- a bounded Homepage product stage, distinct from the PDP atmospheric chamber;
- a Strength / Servings / Purity rail;
- inventory plus `OPENLAB VERIFIED` status;
- product and purchase-handoff actions;
- deterministic Sites-only fixture inputs with an explicit non-runtime-authority boundary.

The reusable component still fails closed unless it receives exactly five products and approved copy. Native Next, C2, Woo, order, payment and deployment state are untouched.

## Gate

Stop at `HUMAN_HOMEPAGE_DESIGN_APPROVAL`. PDP and Native Next adoption do not begin from this receipt.
