# R6 Run Now — Make-ready execution order

**Updated:** 2026-07-27

## Gate result

`R6_FILE_MIGRATION_RECEIPT_V1.status = READY_FOR_MAKE`.

Routes and Make Laboratory now contain the required copied source subtrees. The source file remains untouched. The migration contains 2,422 component instances, zero detached instances, and 22,863 resolved variable bindings. The published source file remains the library authority.

Do **not** publish the empty Production file. Do **not** use Proofs or Archive as Make authority. Do **not** change runtime visual CSS or composition in Codex yet.

## Step 1 — create M0 in native Figma Make

Open Figma Make and create one new Make project named **Olympus R6 Creative Frontier — M0 Light/Dark Material Direction**.

Attach the following *native* source nodes from `nMdvVtpTC3r2JQrhyjQ7yW`:

1. Light hero `1420:843`.
2. Dark hero `1420:813`.
3. Featured Controller candidate workspace `1580:77642`.
4. ProductCommerceCard `1193:4504`.
5. TrustEvidenceSpine `682:3187`.
6. Header Stack page `1169:4859`.
7. Foundations Decision Review page `881:29377`.

Use `550c31d8c5c5d9bf5e6a79c5b64add118243fff9` only as frozen Runtime Studio context. Use ReviewOS session `review-b2f19b7d-ab61-4748-b703-105387b533f9` only as the problem statement. Use MK-2866 as the primary fixture and RAD-140 as the stress fixture.

Paste **Packet M-COMMON** from `R6_FIGMA_MAKE_EXECUTION_RUNBOOK_V2.md`, then paste **Run M0**. Do not paste H1–H5 yet.

M0 must produce three materially different directions for the shared material system: HeaderStack, ProductCommerceCard, Featured Controller, and the OpenLab homepage evidence module. It must show A/B/C at 1440 Light and Dark plus 390 screening. It must return `MAKE_RESULT_V2` and `MATERIAL_DIRECTION_RECEIPT_V1`; its highest allowed status is `RECOMMENDED_CANDIDATE`.

## Step 2 — have Figma Design Agent audit M0

After Make finishes, open the **Lab file** `scM4viMDHV6Jv6Fxccm71g` and retain/copy the M0 candidate frames there. Then run Packet A-REVIEW from the V2 runbook in Figma Design Agent.

The Agent must not redesign the candidates. It must name one recommended material direction, one runner-up, and rejected directions with reasons. It must confirm token/style use, fixture truth, Light/Dark parity, responsive plausibility, accessibility, and absence of A01/Majestic/dashboard language.

## Step 3 — issue a human direction receipt

Choose exactly one result from M0: **Select direction**, **Return to Make**, or **Reject all**. This is a direction decision only; it is not a Codex implementation promotion.

If you select a direction, issue `HUMAN_DIRECTION_RECEIPT` with the chosen Make node, the material rules to preserve, the rejected rules, and the reason. Only then may H1 begin.

## Step 4 — run Homepage as five bounded Make runs

Run these in sequence, not as one large Homepage prompt:

1. **H1:** Hero narrative, Featured Controller, and selector.
2. **H2:** Category Discovery cards.
3. **H3:** ProductGrid / Most Ordered density and card treatment.
4. **H4:** OpenLab homepage evidence module.
5. **H5:** Homepage cohesion assembly using the accepted H1–H4 outputs.

For H1–H4, Make produces A/B/C, Figma Agent audits them, and you issue a human direction receipt. For H5, the Agent produces `FIGMA_AGENT_CHAMPION_RECEIPT_V2`; you then either issue `HUMAN_PROMOTION_RECEIPT`, return the route to Make, or reject it.

## Step 5 — only then hand one promoted Homepage candidate to Codex

Codex receives exactly three files/receipts: `MAKE_RESULT_V2`, `FIGMA_AGENT_CHAMPION_RECEIPT_V2`, and `HUMAN_PROMOTION_RECEIPT`. Codex implements only the named H5 candidate, then Runtime Studio renders the required 1440/1024/390 × Light/Dark proof and ReviewOS accepts, revises, or rejects it.

## Step 6 — run remaining route families only after the first full loop is accepted

Run Product in this order: P1 QuickAdd, P2 PurchasePanel, P3 LabReportPanel, P4 Fold-Out Dossier, P5 Related Products, P6 PDP assembly.

Then run Catalogue/Search: C1 catalogue density, C2 search results/empty state, C3 category landing, C4 route assembly.

Then run OpenLab: O1 portal, O2 lab report template, O3 dossier/record, O4 methodology/source chain, O5 compare, O6 route assembly.

Run Checkout and Account only after those waves. Checkout remains security constrained: Figma may redesign its visual shell and information hierarchy, but must not alter payment-provider or commerce mutation boundaries.

## Deferred split work

Production migration runs last, after a pre/post variable/style/component-key manifest and parity receipt. Proofs and Archive are evidence/history migrations and can happen later; neither blocks M0, Homepage, Product, Catalogue, or OpenLab Make work.
