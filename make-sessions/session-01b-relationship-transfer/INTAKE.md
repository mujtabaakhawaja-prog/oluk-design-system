# MF-01B Cross-Context Surface Relationship Frontier — Intake

**Status:** BLOCKED — awaiting MF-01A relationship selection and real exports
**Purpose:** Prove that selected ProductCommerceCard material relationships survive materially different customer-facing jobs before a card champion or system grammar is selected.

## Boundary

MF-01B is a PNG-led relationship-transfer run. It is not a full homepage, PDP, evidence route, component library, token exercise, runtime prototype or native-node reconstruction.

Do not create placeholder image files or seal hashes until the real MF-01A selection and reference exports exist.

## Planned attachment slots

1. `mf01a-selected-relationships.png` — human-curated relationship summary from the three MF-01A hypotheses.
2. `g0-canonical-canvas-rhythm.png` — curated crop from G0 canonical reference node `354:5499`; spacing, density, silhouette and self-elevating-plane evidence only.
3. `homepage-hero-banner.png` — homepage entry relationship from Runtime Proofs node `12:66`; arrangement evidence, not identity or copy authority.
4. `pdp-section-1-fullfield.png` — flat atmospheric first-fold reference from Final-Design node `126:37`.
5. `lab-verification-embedded.png` — connected embedded-divider evidence reference from Final-Design node `188:450`.
6. `labreports-canvas-split.png` — true canvas-separated editorial/ledger reference from Final-Design node `222:1332`.

All six inputs must be flattened PNG evidence. Native Figma nodes, design-system boards, entire Make files and repository-wide image sets remain out of scope.

## Required transfer proof

Produce two coherent material-relationship hypotheses that each demonstrate:

1. a distinctive homepage entry silhouette;
2. the PDP flat atmospheric exception with an independently raised purchase plane;
3. one connected evidence object using an embedded divider;
4. one editorial/rail composition using exposed canvas between independent jobs.

The hypotheses must preserve the accepted surface decision test:

- one semantic object → embedded divider;
- independent peer jobs → canvas split;
- authorized PDP first fold → flat atmospheric exception;
- gap that adds no hierarchy → remove as deadspacing.

## Exit

MF-01B exits when a human records which relationships transfer successfully and which fail by context. It does not select final tokens, publish a surface grammar, reconstruct full routes or introduce native components. The accepted MF-01A and MF-01B relationships become inputs to MF-02.
