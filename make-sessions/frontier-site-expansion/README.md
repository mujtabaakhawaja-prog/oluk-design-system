# OLUK Frontier Make Kit — Canonical Bulk Run

**Status:** `PRIVATE_CREATIVE_FRONTIER · HUMAN_REVIEW_REQUIRED · UNPUBLISHED`

This kit lets Figma Make explore the remaining modular site programme in parallel with Codex Sites. It starts from the corrected cumulative Sites candidate and the consolidated Final-Design library. It is not a restart, a screenshot-tracing exercise, or a second component system.

## Current baseline — do not redo

The cumulative correction at `0df3ad0` already implements:

- Direction D `LockedHero` from `1155:29963`, with the `462:4684` 5-3-1 product stage and local interaction.
- PDP Section 1 from `1155:30632`.
- OpenLab `HeroLight` from `614:75995`.
- OptionB Premium three-band header from `1176:28930`, including hydrated menus and compact mobile navigation.
- mountain-mark footer from `754:18226`.
- contained Bag/checkout payment-trust rails with the inherited GBP/USD lifecycle language.

The supplied full-resolution RAD-140 master is repository-owned at `assets/rad-140-master.png`; Sites uses the visually identical delivery derivative at `public/assets/products/rad-140/front.png`. It visibly carries `8 MG · 60 SERVINGS · >99%`. Do not use the retired shop thumbnail or historical `front-design-fixture.png`.

Final-Design Page 09 (`672:10`) is reported at 27 component sets + 5 standalone components. The OptionB header and three TrustEvidenceSpine components have been promoted; 86 split instances were repointed and 12 dead duplicate definitions removed. Treat those as completed consolidation work, not new Make tasks.

## What is genuinely pending

1. Rebuild the legacy `module-a-your-stack-desktop` (`1081:28849`) from canonical components; its three locally drawn product cards are not system-bound.
2. Explore and backfill a SHOP mega-menu, OPEN LAB mega-menu, mobile navigation sheet, and contextual secondary navigation as reusable Figma candidates matching the implemented OptionB interaction model.
3. Finish reusable growth/account, checkout lifecycle, OpenLab tool, and supporting-route modules from the bulk batches in `PROMPTS.md`.
4. Triage—do not blindly delete—the remaining workspace and legacy component definitions after their references are classified.
5. Return selected relationships to Review Studio and the bridge; Make output never bypasses canonical implementation.

## Non-negotiable build order

For every generated object:

1. canonical component provenance;
2. governed token/material/type/layout law;
3. actual approved product-render binding;
4. desktop and compact mobile composition;
5. module mount role across routes;
6. customer-facing editorial finish.

If steps 1–4 fail, rebuild the object before polishing copy. Supplied product, trust, evidence, and checkout language is already accepted creative input for this frontier; preserve it. Do not weaken the design with governance labels, placeholder prose, or “this goes here” annotations.

## First run — start here

Run [`runs/01-canonical-your-stack/PROMPT.md`](runs/01-canonical-your-stack/PROMPT.md) before the broader batches. It rebuilds the only currently identified growth module that still bypasses the canonical component graph: `module-a-your-stack-desktop` (`1081:28849`).

The run folder is self-contained and includes its own attachment instructions, interactive customer-facing `app.tsx`, render-bound product data, and three repository product assets. It asks for three materially different directions at 1440 and 390; it does not ask Make for a system board or developer proof.

## Bulk execution after Run 01

`PROMPTS.md` contains six independent Make batches. Run batches in separate Make sessions so one large generation cannot flatten every module into the same layout. Batch E consumes the relationship selected from Run 01 rather than regenerating the legacy Your Stack object.

| Batch | Focus | Output |
|---|---|---|
| A | shell and navigation | desktop mega menus + 390 navigation sheet + contextual rail |
| B | PDP and commerce decision | product decision, details, evidence, continuation |
| C | OpenLab | portal, record, dossier, tools, technical content |
| D | checkout/post-purchase | full lifecycle, equality lock, receipt/tracking/refund |
| E | growth/account | canonical Your Stack, restock, loyalty, account continuation |
| F | supporting/73-route composition | support, content, category and route-family templates |

Each batch returns:

- one 1440 composition;
- one genuinely compact 390 composition;
- canonical component inventory used;
- new component proposals, if any;
- cross-mount table;
- desktop/mobile order and disclosure decisions;
- rejected local redraws;
- a short selection note.

## Inputs

- Attach or reference only the actual renders in `assets.json` for products shown.
- Use `app.tsx` as the visual/material baseline, not as runtime source.
- Use the exact Figma nodes in `assets.json` for the already-selected major surfaces.
- Use Plus Jakarta Sans for display and Inter Variable for UI/body.
- Light mode only; the footer is the sole inverse surface.

## Handoff

1. Generate the batch.
2. Select relationships, not an entire Make codebase.
3. Record selected frames and component candidates.
4. Codex rebuilds them from the canonical graph in Sites.
5. Review the affected module group and consuming route families in `/review-studio` at 1440 and 390.
6. Only after selection, backfill a reusable Figma component and update the bridge.

No output from this kit publishes a library, deploys Sites, mutates commerce, or becomes Shopper runtime code.
