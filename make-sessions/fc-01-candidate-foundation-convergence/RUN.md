# FC-01 — Runbook

**Status:** `ADAPTIVE_CARD_FAMILY_TECHNICAL_EVIDENCE_READY / DOSSIER_CORRECTION_REQUIRED / HUMAN_SELECTION_REQUIRED`

## P0 — Discovery and containment

- inspect repository authority in required order;
- inspect current Sites tokens/components/tests;
- inspect Figma pages, variables, styles and component masters;
- inspect subscribed libraries before creating anything;
- record all conflicts in the authority matrix;
- confirm cumulative human decisions resolve them.

**Historical P0 exit:** inventory, authority map, decisions, review map and receipt were recorded before bounded candidate artifacts were created. `COMPLETE`; later technical evidence is governed by the sections below.

## Adaptive candidate technical evidence — complete

1. Hidden unpublished Figma candidate foundation and adaptive sets created.
2. Explicit responsive variants registered at 1440/1024/768/390.
3. Route-isolated Sites `/review` wrapper and typed fixtures implemented.
4. Browser geometry, wrapping, focus, image and console checks passed locally.
5. Build-backed test suite passed `6/6`.

## Human-selection run — next

1. Open `637:3`, `646:10801`, `646:10802`, `639:13888`, `643:8616`, `639:13889` and the four review frames.
2. Open the owner-only `/review` anchors in `REVIEW-MAP.md` after the exact-commit private deployment receipt is recorded.
3. Compare component anatomy, state coverage and responsive recomposition; do not evaluate the rejected customer routes as the candidate.
4. Record one explicit verdict: `SELECT`, `CORRECTION_REQUIRED` or `REJECT`.
5. Leave the dossier and full related rail open unless their separate acceptance checks pass.

## Held convergence sequence — requires later explicit authorization

If and only if the human selection is recorded and the next gate opens:

1. correct the dossier and close the full related rail;
2. converge selected customer sections through shared primitives;
3. validate build, lint, TypeScript, route health, computed styles, keyboard/focus, four-width captures and child bounds;
4. seek foundation promotion separately.

`overflow-x: clip` is not proof. Any child outside its intended parent is a failure even when page scroll width is unchanged.

## Current stop condition

One exact-commit owner-only candidate deployment is now authorized for this review gate. Do not alter owner-only access, import the candidate layer into customer routes, repair unrelated surfaces, or promote a foundation. Stop after recording the private deployment receipt and presenting the human selection surface.
