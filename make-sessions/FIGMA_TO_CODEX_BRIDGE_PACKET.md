# GATED POINTER — NOT AN ACTIVE MAKE INPUT

This legacy bridge previously declared visual choices implementation-ready
before human selection and mixed creative Make work with runtime sequencing.
That path is superseded.

## Active creative run

Use:

`make-runs/light-identity-authority-v1/`

Only its `guidelines.md` and `PROMPT.md` enter the foundation Make context.

## Post-champion sequence

After the human review gate locks the light foundation:

1. Run the appropriate completion packets from
   `make-runs/light-identity-authority-v1/COMPLETION_RUNS_01_10.md`.
2. Execute and review the deterministic capture lane in
   `make-runs/light-identity-authority-v1/LOCALHOST_CAPTURE_PIPELINE.md`.
3. Load `post-champion/A2_FIGMA_ADOPTION.md` for native Figma systemization.
4. Load `post-champion/A3_CODEX_RUNTIME_BRIDGE.md` for selective production
   implementation.

No exact spacing value, component API, runtime contract or token mapping in
this repository is considered final until the accepted visual output has been
measured and adopted natively.

