# MF-03+ — Convergent prompt catalog

Use exactly one run block at a time. Copy all three headings into the agent prompt. Never shorten `INHERITED_CHAMPION_STATE` to “use the latest board.”

## Shared prompt preamble — required for every run

### INHERITED_CHAMPION_STATE

- `authority/DECISION-LEDGER.json` is the append-only decision authority. The latest accepted event wins; proposals do not control.
- MF-01A node `626:11760` remains the persistent material/anatomy/state reservoir. Carry its accepted relationships forward before applying this run's delta.
- Use exact product truth: `SARM SERIES · MK-2866 · Ostarine · SKU 80529-01 · 15 MG · 90 SERVINGS · >99% · £43`.
- Use the canonical candidate components registered in `authority/ARTIFACT-REGISTRY.json`; instantiate them rather than recreating page-local copies.
- Hero: `717:15468` controls layout/composition only, after the `90 SERVINGS` correction. `626:7994` contributes complete product/status/evidence data. The target is the purpose-built `HeroDecisionSurface`/`ProductDecisionHero`, not a raw frame or whole-zone wrapper.
- `713:1065` is a Hero component with PurchasePanel geometry, not Compact. Preserve that classification only as provenance; do not promote the structure.
- Keep/extend 86 convergence variables. Archived 128 legacy/quarantined variables are read-only provenance.
- Use graduated single shadows: Compact y5/blur12/.09; Vertical/Featured y24/blur60/.10; PurchasePanel y20/blur50/.18; Relation y12/blur25/.12.
- Footer is the sole inverse surface. All other customer surfaces remain in the light system.
- The 12px metadata / 15–16px body floor is `PROPOSED_NON_CONTROLLING`; report violations for review but do not claim this proposal as champion authority.
- Everything produced by this run is `CANDIDATE · HUMAN_REVIEW_REQUIRED · UNPUBLISHED`.

### THIS_RUN_DELTA

Replace this section with exactly one delta below. Change only the named surface and the smallest shared component correction required to make that surface coherent.

### DO_NOT_INHERIT

- Do not use an MF-02/MF-03 board as a full replacement state. Inherit champion state first; take only the explicitly named delta.
- Do not inherit `90 CAPS`, clipped text, fixed-width tracks, raw page-local variables, generic shadows, cobalt card perimeters/top edges, undersized source type, broad inverse slabs, or dashboard styling by default.
- Do not redraw canonical component anatomy inside a page.
- Do not publish variables/components, create Code Connect mappings, mutate runtime ownership, connect inventory/cart/payment APIs, or expose development labels in customer UI.
- Do not call technical validation or an agent verdict “approved.” Only the champion user's artifact-specific review can promote it.

---

## MF-03A delta — shell and homepage (P0)

Build one customer-facing homepage candidate from canonical instances:

1. `SiteHeader`.
2. `ProductDecisionHero` using the reconciled hero rules above.
3. `TrustRail`.
4. A commerce/discovery section that demonstrates Featured, Vertical/Compact where contextually useful, Relation and Assurance relationships without card soup.
5. `SiteFooter` as the sole inverse surface.

Preserve the commerce taxonomy `SARMs / Prohormones / Research Chemicals / Stacks`. Keep exposed canvas as structural margin/rhythm, not a tinted full-width section alternator. Produce desktop/tablet/mobile variants or a responsive implementation at 1440/1024/768/390. Return a node/route receipt and every deviation from inherited authority.

## MF-03B delta — OpenLab portal (P0)

Build one light-mode OpenLab portal that is editorial evidence navigation, not a generic dashboard and not a second design language. Reuse `SiteHeader`, `TrustRail`, `AssuranceRail`, `Dossier`, Relation/commerce bridge and `SiteFooter`. Use the information architecture tabs/labels `TECHNICAL · PRODUCT EVIDENCE · COMMERCE` only where they are navigation or static indexing; do not invent unsupported interaction semantics.

The portal must lead toward archive, lookup, record and product dossier work without pretending fixtures are live evidence. Return a node/route receipt, fixture/runtime-owner classification and deviations.

## MF-04 delta — PDP (P0)

Build the MK-2866 product detail candidate:

1. canonical header;
2. full-field first-fold media relationship with a separately raised canonical `PurchasePanel`—never an elevated outer hero card;
3. exact product truth and evidence/status atoms;
4. canonical `TrustRail`;
5. canonical `Dossier` as the agreed Product/Facts/Composition presentation;
6. canonical `AssuranceRail`;
7. canonical `RelatedRail`;
8. sole-inverse footer.

The first fold is the only atmospheric full-field exception. Keep product media authored and content accessible at all four widths. Return a node/route receipt and deviations.

## MF-05 delta — dossier and evidence transitions (P1)

Prove how the PDP moves from commerce decision to progressively denser evidence. Use the canonical Dossier and Assurance relationships, then design the joined embedded-divider and true canvas-split evidence states. Maintain one visual language, exact product truth and clear fixture/runtime separation. Do not turn the page into a dashboard. Test 1440/1024/768/390 and return transition-state receipts.

## MF-05A delta — decision surfaces (P2)

Explore compare, bundle, stack builder and goal-led discovery only after card/evidence relationships are stable. Produce distinct decision jobs, not differently titled copies of the same card grid. Remain light mode, preserve the footer-only inverse rule and cap any presentation-only discount at 25%. No cart/payment plumbing. Return a relationship selection board or private route candidate plus deviations.

## MF-06 delta — OpenLab archive lifecycle (P1)

Extend MF-03B into portal → archive/search → batch lookup → selected record → product dossier. Reuse the same shell, evidence grammar and fixture/runtime-owner labels internally while keeping governance terms out of customer UI. Provide empty, populated, no-result and unavailable presentation states without fabricating published evidence. Return a route map and four-width receipts.

## MF-07 delta — transaction presentation (P1)

Design bag, checkout details, delivery, payment handoff, order-pay, confirmation, failure and retry as presentation-only surfaces. Preserve product truth, the light system and footer-only inverse rule. Do not modify WooCommerce, Shopper SSR, C2, Initiator or payment ownership. Return inert/static prototypes and a separate runtime handoff map.

## MF-09 delta — responsive and local-state proof (P0)

Test the reviewed MF-03A/B, MF-04 and any accepted P1 candidates at 1440/1024/768/390. Exercise hover, keyboard focus, quantity, added, unavailable, out-of-stock, search/filter and record reveal where applicable. Fail any width with clipping, overlap, hidden overflow or semantic boundary escape. Do not solve geometry with `overflow-x: clip`. Return a per-route/per-width matrix.

## MF-10 delta — final convergence gate (P0)

Assemble the reviewed homepage, Shop, PDP, OpenLab and required presentation lifecycle into one coherent light-mode system. Audit component provenance, variable bindings, product/evidence truth, sole-inverse footer, route differentiation, accessibility and four-width behavior. List every unresolved P1/P2 item. Do not publish or promote; produce the artifact-specific approval board and final receipt for the champion user.

## MF++ delta — promotion and runtime translation (blocked)

Run only after MF-10 champion approval and separate explicit authorization. Split work into independently reviewable packets: Figma library promotion, Code Connect, Codex Sites/customer-route adoption, Shopper SSR/Woo adapters, evidence runtime, checkout/payment/C2 integration, telemetry and release. Never infer runtime ownership from Figma fixtures.
