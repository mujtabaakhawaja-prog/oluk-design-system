# OLUK R6 Light Identity Authority V1

## File strategy

Use a fresh Figma Make project named:

`OLUK R6 — Light Identity Authority V1`

The existing R6 Design file remains the visual evidence source, the home of the
curated authority packet and the eventual destination for accepted work:

`yNAyIQhewnbofeZWMGkKVa` — OLUK R6 — Codex Runtime Proofs

Do not continue either prior Make run. Do not create a second design-source
file. PASS80 is a process precedent only; it is not a visual or brand source.

Add one page in the existing R6 Design file:

`R6 / LIGHT IDENTITY AUTHORITY PACKET`

Assemble the four compact boards defined in `SOURCE_MANIFEST.md` from the
existing nodes. This is curation, not a redesign. Do not attach the scattered
source pages wholesale.

After human review, place the accepted result back in the existing R6 Design
file on a new page named:

`LIGHT IDENTITY AUTHORITY — MAKE V1`

## Run setup

1. Create the fresh Make project.
2. Add only `guidelines.md` to the project guidelines.
3. Copy and attach the four curated boards listed in `SOURCE_MANIFEST.md`.
4. Paste `PROMPT.md` as the first run.
5. Review the result with `REVIEW_GATE.md`.
6. Do not load any `post-champion/` file until a human locks the foundation.
7. After lock, run packets 01–08 in the same Make project. Take a duplicate or
   version snapshot before each packet.
8. Run packet 09 in the local browser-capture lane.
9. Run packet 10 in native Figma Design and Codex after visual QA passes.

## What this run replaces

This is the active path. It supersedes the blank five-direction Product Card
session and the five-session primitive-first roadmap. Those files remain only
as historical receipts and are marked `SUPERSEDED — DO NOT RUN`.

## Authority boundary

The foundation run decides visual identity and compositional grammar. It does
not decide React architecture, SSR, TypeScript contracts, Tailwind structure,
route behavior, breakpoints, telemetry, or production state management.
