import { createHash } from "node:crypto";
import { readdirSync, readFileSync, writeFileSync } from "node:fs";
import { join, relative, resolve } from "node:path";
const root=resolve(import.meta.dirname,"..");const sourceDir=join(root,"app/design-system");const output=resolve(root,"../../authority/generated/OLUK-COMPONENT-CENSUS-V1.json");const files=readdirSync(sourceDir).filter((name)=>name.endsWith(".tsx")).sort();
const familyFor=(file)=>file.includes("header")||file.includes("navigation")?"shell":file.includes("openlab")||file.includes("evidence")?"openlab":file.includes("product")||file.includes("commerce")||file.includes("metric")||file.includes("price")?"commerce":file.includes("pdp")||file.includes("dossier")?"pdp":file.includes("review")||file.includes("atlas")?"owner_tooling":file.includes("surface")||file.includes("token")||file.includes("action")||file.includes("status")?"foundation":"route_module";
const components=[];for(const file of files){const source=readFileSync(join(sourceDir,file),"utf8");for(const match of source.matchAll(/export\s+(?:function|const)\s+([A-Z][A-Za-z0-9_]*)/g)){const name=match[1];components.push({id:`component.${name.replace(/([a-z0-9])([A-Z])/g,"$1-$2").toLowerCase()}`,name,family:familyFor(file),variants:name.includes("Card")?["default","compact","vertical","featured","relation"]:["default"],states:["default","focus-visible","mobile"],responsiveModes:[1440,1024,768,390],pageTemplates:name.includes("Pdp")||name.includes("Product")?["product-detail"]:name.includes("OpenLab")?["openlab-portal","openlab-archive"]:["shared"],routeModuleUsage:[],governingSource:relative(root,join(sourceDir,file)),stagingImplementation:"implemented",runtimeStudioAdoption:"not_assessed",openGates:["human_review"]});}}
components.sort((a,b)=>a.id.localeCompare(b.id));const families=[...new Set(components.map((entry)=>entry.family))].sort();const byFamily=Object.fromEntries(families.map((family)=>[family,components.filter((entry)=>entry.family===family).length]));const content={contract:"OLUK_COMPONENT_CENSUS_V1",generatedFrom:"sites/oluk-experience-lab/app/design-system/*.tsx",counts:{components:components.length,byFamily},components};const contentHash=createHash("sha256").update(JSON.stringify(content)).digest("hex");const serialized=`${JSON.stringify({...content,contentHash},null,2)}\n`;if(process.argv.includes("--check")){if(readFileSync(output,"utf8")!==serialized)throw new Error("OLUK component census is stale");console.log(`OLUK component census verified: ${components.length} components · ${contentHash}`);}else{writeFileSync(output,serialized);console.log(`OLUK component census generated: ${components.length} components · ${contentHash}`);}
