import "../design-system/candidate-tokens.css";
import "../design-system/candidate-review.css";

export default function ReviewLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
