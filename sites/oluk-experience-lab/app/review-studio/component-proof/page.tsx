import { ComponentProofClient } from "./component-proof-client";

/** Owner-only responsive specimen review. It carries no publication or runtime authority. */
export default function ComponentProofPage() {
  return <ComponentProofClient />;
}
